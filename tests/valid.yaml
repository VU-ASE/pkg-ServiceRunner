# Service definitions
name: mytest
description: This is my first service

# Define dependencies
dependencies:
  - service: controller
    output: actuators

# Define outputs
outputs:
  - name: actuators
    address: tcp://*:5555
