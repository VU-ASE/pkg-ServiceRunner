# Service definitions
name: basicregistration
description: A service to test basic registration

# Define dependencies
dependencies:
  - service: myfirstdependencyservice
    output: myfirstdependencyoutput

# Define outputs
outputs:
  - name: testoutput
    address: tcp://localhost:5555
