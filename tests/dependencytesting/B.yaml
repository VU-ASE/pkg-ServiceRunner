#
# This test has a service that depends on service B, which has no dependencies
#

# Service definitions
name: serviceB
description: A service to test dependency resolution

# Define dependencies
dependencies: []

# Define outputs
outputs:
  - name: output1
    address: tcp://localhost:5555
